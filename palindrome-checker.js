function palindrome(str) {
  //remove whitespaces
  // remove non alphabetical characters
  // transform to lowercase
  strLowered = str.toLowerCase();
  console.log(strLowered);
  return true;
}

palindrome("Is thIS worKinG?");
